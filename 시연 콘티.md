# DebtCrasher 시연 콘티

예상 시연 시간: 약 1분 30초 ~ 3분

## 준비 사항

1. VS Code에 DebtCrasher 확장이 로드되어 있어야 한다.
2. 빈 폴더 또는 간단한 프로젝트 폴더를 워크스페이스로 연다.
3. API 키가 설정되어 있어야 한다 (또는 데모 모드 사용).
4. Activity Bar에서 DebtCrasher 아이콘이 보이는 상태에서 시작한다.

## 시연 흐름

### 1. 화면 소개 (10초)

**보여줄 것**: VS Code 좌측 Activity Bar → DebtCrasher 아이콘 클릭 → Agent View 열림

**말할 것**:
> "DebtCrasher는 VS Code 확장 프로그램으로, 왼쪽에 Agent View와 Step View 두 개의 패널이 있습니다."

---

### 2. 요청 입력 (10초)

**보여줄 것**: Agent View의 입력란에 다음 텍스트를 입력하고 전송

```
TODO 저장 기능 추가
```

이것은 발표문 슬라이드 7의 "저장 기능 예시"와 동일한 맥락이다. "저장 기능"이라는 짧은 요청 안에 숨어 있는 판단 지점들을 DebtCrasher가 질문으로 드러내는 과정을 보여준다.

> ※ 데모 모드를 사용하면 API 호출 없이 미리 준비된 시드 데이터로 동일한 흐름을 보여줄 수 있다. 시연 안정성을 위해 데모 모드를 추천한다.

**말할 것**:
> "개발자가 'TODO 저장 기능 추가'라고만 요청했습니다. 짧은 요청이지만, 실제로는 정해야 할 판단이 여러 개 숨어 있습니다."

---

### 3. Planning Gate — 질문 확인 (30초)

**보여줄 것**: AI가 생성한 질문 목록이 나타남. 다음 질문들이 표시된다:

| 질문 | 핵심 포인트 |
|---|---|
| **TODO 저장 위치** | workspace JSON / globalState / 외부 DB 중 선택 |
| **TODO 명령 public contract** | 최소 명령 / CRUD 전체 중 선택 |
| **저장 실패 시 처리** | 무시 / 알림 / 백업 중 선택 |

**말할 것**:
> "DebtCrasher가 이 요청에서 개발자가 먼저 정해야 할 판단 지점들을 질문으로 보여주고 있습니다."
>
> "첫 번째 질문은 TODO 데이터를 어디에 저장할지입니다. 이건 데이터 지속성과 손실 가능성에 직접 영향을 주는 판단이라 REVIEW_REQUIRED로 분류되었습니다."
>
> "AI가 알아서 정할 수도 있지만, 그러면 이 판단이 내 경험으로 남지 않습니다."

---

### 4. 사용자 선택 (20초)

**보여줄 것**: 각 질문에 선택지를 클릭해서 답변

- 질문 1: **workspace 내부 JSON 파일** 선택
- 질문 2: **add/list/toggle 최소 명령** 선택
- 질문 3: (있는 경우) 적절한 선택지 선택
- "개발 시작" 버튼 클릭

**말할 것**:
> "이렇게 각 질문에 답하면서, 개발자가 직접 구현 방향을 정하게 됩니다. 이 선택들은 자동으로 DECISIONS.md에 기록됩니다."

---

### 5. 구현 결과 확인 (20초)

**보여줄 것**: 구현 결과 화면

- 생성된 파일 목록 (또는 demo preview)
- 검증 결과 (typecheck passed 등)
- DECISIONS.md, AGENT.md 업데이트 안내

**말할 것**:
> "선택이 끝나면 DebtCrasher가 그 판단을 기반으로 코드를 생성하고, 가능한 검증 명령도 자동으로 실행합니다."
>
> "결과와 함께 DECISIONS.md에 어떤 질문에 무엇을 선택했는지가 기록되고, AGENT.md에 프로젝트 요약이 캐시됩니다."

---

### 6. Step View — 학습 자료 생성 (30초)

**보여줄 것**: 좌측 하단 Step View로 전환

1. 결정 기록 목록에서 방금 만든 결정들이 보임
2. 체크박스로 선택
3. 학습 자료 생성 버튼(노트북 아이콘) 클릭
4. 생성된 Markdown 문서가 열림

**말할 것**:
> "Step View에서는 지금까지 기록된 결정들을 볼 수 있습니다."
>
> "선택해서 학습 자료 생성을 누르면, 어떤 판단이 있었는지, 무엇을 선택했는지, 검증 결과는 어땠는지를 정리한 문서가 만들어집니다."
>
> "이 문서는 AI가 내용을 추가하거나 해석한 게 아니라, 기록된 데이터를 보기 편한 형태로 정리한 것입니다."

---

### 7. 마무리 (10초)

**말할 것**:
> "이렇게 질문으로 판단을 드러내고, 선택을 기록하고, 나중에 다시 볼 수 있는 자료로 만드는 게 DebtCrasher의 핵심 흐름입니다."

---

## 데모 모드 vs 실제 모드

| 항목 | 데모 모드 | 실제 모드 |
|---|---|---|
| API 호출 | 없음 (시드 데이터 사용) | 있음 |
| 파일 생성 | 없음 (preview만) | 실제 파일 작성 |
| 검증 | 시드 결과 표시 | 실제 명령 실행 |
| 장점 | 안정적, 일관된 시연 | 실제 동작 증명 |
| 위험 | 실제 동작이 아님이 드러남 | API 실패/지연 가능 |

**추천**: 발표에서는 **데모 모드로 흐름을 보여준 뒤**, 시간이 남으면 "실제로도 동일하게 동작합니다"라고 언급하는 방식.

---

## 예상 질문 대비

### Q. AI 질문이 항상 맞나요?
> "항상 최적이라고 보장하지는 않습니다. 하지만 저희 프로젝트의 목적은 AI 질문의 정확성보다, 개발자가 판단을 직접 해보는 연습 기회를 주는 것입니다. 질문이 완벽하지 않더라도 '이런 판단 지점이 있었구나'를 알게 되는 것 자체가 의미 있다고 봤습니다."

### Q. 기존 AI 코딩 도구와 뭐가 다른가요?
> "Copilot이나 Cursor 같은 도구는 코드를 바로 생성합니다. DebtCrasher는 코드 생성 전에 '이건 정하고 넘어가야 합니다'라는 단계를 하나 추가합니다. 그리고 그 선택을 기록해서 나중에 다시 볼 수 있게 합니다."

### Q. 질문이 너무 많으면 귀찮지 않나요?
> "초기에 실제로 그 문제를 겪었습니다. 그래서 Human Review Gate를 도입해서 보안, 데이터 손실, 되돌리기 어려운 변경만 묻고 나머지는 자동으로 기록하는 방식으로 줄였습니다. 요구사항이 구체적일수록 질문도 자연스럽게 줄어듭니다."

### Q. 학습 자료에 AI가 해석을 추가하지 않으면 내용이 부족하지 않나요?
> "현재는 기록된 데이터를 그대로 정리하는 수준입니다. 이후에 RAG나 검증 기반 신뢰성 보완을 적용할 예정이지만, 지금 단계에서는 '이 판단이 있었고 다음에 확인해야 할 점은 이것이다' 정도만 알 수 있으면 충분하다고 봤습니다."

### Q. 나중에 도구 없이도 할 수 있게 되나요?
> "네, 연습용 젓가락처럼 처음에 감을 잡게 해주는 도구입니다. 어떤 판단을 요구사항에 넣어야 하는지 습관이 생기면 도구 없이도 구체적인 요청을 쓸 수 있게 됩니다."
